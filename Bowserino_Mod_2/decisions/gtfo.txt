political_decisions = {
	gtfo_on = {
		picture = gtfo
		alert = no
		potential = {
			OR = {
				has_country_flag = government_decisions_on
				ai = yes
			}
			num_of_vassals_no_substates = 1
			is_vassal = no
			NOT = { has_country_flag = gtfo has_country_flag = gtfo_disabled }
		}
		allow = { civilized = yes ai = no }
		effect = { set_country_flag = gtfo }
	}
	gtfo_off = {
		picture = gtfo
		alert = no
		potential = {
			has_country_flag = gtfo
			NOT = { has_country_flag = gtfo_disabled }
		}
		allow = { ai = no }
		effect = { clr_country_flag = gtfo }
	}
	gtfo_disabled = {
		picture = gtfo
		alert = no
		potential = {
			OR = {
				has_country_flag = government_decisions_on
				ai = yes
			}
			has_country_flag = gtfo
			NOT = { has_country_flag = gtfo_disabled }
		}
		allow = { ai = no }
		effect = { clr_country_flag = gtfo set_country_flag = gtfo_disabled }
	}

	gtfo_ADA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ADA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ADA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ADA
			diplomatic_influence = { who = ADA value = -100 }
		}
	}

	gtfo_ADE = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ADE = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ADE = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ADE
			diplomatic_influence = { who = ADE value = -100 }
		}
	}

	gtfo_ADU = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ADU = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ADU = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ADU
			diplomatic_influence = { who = ADU value = -100 }
		}
	}

	gtfo_AFG = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			AFG = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			AFG = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = AFG
			diplomatic_influence = { who = AFG value = -100 }
		}
	}

	gtfo_AIN = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			AIN = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			AIN = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = AIN
			diplomatic_influence = { who = AIN value = -100 }
		}
	}

	gtfo_AKM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			AKM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			AKM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = AKM
			diplomatic_influence = { who = AKM value = -100 }
		}
	}

	gtfo_ALD = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ALD = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ALD = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ALD
			diplomatic_influence = { who = ALD value = -100 }
		}
	}

	gtfo_ALH = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ALH = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ALH = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ALH
			diplomatic_influence = { who = ALH value = -100 }
		}
	}

	gtfo_AND = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			AND = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			AND = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = AND
			diplomatic_influence = { who = AND value = -100 }
		}
	}

	gtfo_ARA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ARA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ARA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ARA
			diplomatic_influence = { who = ARA value = -100 }
		}
	}

	gtfo_ARG = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ARG = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ARG = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ARG
			diplomatic_influence = { who = ARG value = -100 }
		}
	}

	gtfo_ARK = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ARK = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ARK = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ARK
			diplomatic_influence = { who = ARK value = -100 }
		}
	}

	gtfo_ARM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ARM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ARM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ARM
			diplomatic_influence = { who = ARM value = -100 }
		}
	}

	gtfo_ASH = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ASH = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ASH = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ASH
			diplomatic_influence = { who = ASH value = -100 }
		}
	}

	gtfo_ASM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ASM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ASM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ASM
			diplomatic_influence = { who = ASM value = -100 }
		}
	}

	gtfo_ATJ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ATJ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ATJ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ATJ
			diplomatic_influence = { who = ATJ value = -100 }
		}
	}

	gtfo_AUS = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			AUS = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			AUS = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = AUS
			diplomatic_influence = { who = AUS value = -100 }
		}
	}

	gtfo_AVA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			AVA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			AVA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = AVA
			diplomatic_influence = { who = AVA value = -100 }
		}
	}

	gtfo_AWA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			AWA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			AWA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = AWA
			diplomatic_influence = { who = AWA value = -100 }
		}
	}

	gtfo_AYR = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			AYR = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			AYR = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = AYR
			diplomatic_influence = { who = AYR value = -100 }
		}
	}

	gtfo_AZT = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			AZT = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			AZT = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = AZT
			diplomatic_influence = { who = AZT value = -100 }
		}
	}

	gtfo_BAL = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BAL = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BAL = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BAL
			diplomatic_influence = { who = BAL value = -100 }
		}
	}

	gtfo_BAV = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BAV = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BAV = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BAV
			diplomatic_influence = { who = BAV value = -100 }
		}
	}

	gtfo_BER = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BER = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BER = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BER
			diplomatic_influence = { who = BER value = -100 }
		}
	}

	gtfo_BEY = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BEY = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BEY = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BEY
			diplomatic_influence = { who = BEY value = -100 }
		}
	}

	gtfo_BHU = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BHU = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BHU = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BHU
			diplomatic_influence = { who = BHU value = -100 }
		}
	}

	gtfo_BLC = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BLC = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BLC = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BLC
			diplomatic_influence = { who = BLC value = -100 }
		}
	}

	gtfo_BLM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BLM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BLM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BLM
			diplomatic_influence = { who = BLM value = -100 }
		}
	}

	gtfo_BNG = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BNG = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BNG = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BNG
			diplomatic_influence = { who = BNG value = -100 }
		}
	}

	gtfo_BNY = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BNY = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BNY = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BNY
			diplomatic_influence = { who = BNY value = -100 }
		}
	}

	gtfo_BOH = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BOH = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BOH = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BOH
			diplomatic_influence = { who = BOH value = -100 }
		}
	}

	gtfo_BOS = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BOS = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BOS = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BOS
			diplomatic_influence = { who = BOS value = -100 }
		}
	}

	gtfo_BOU = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BOU = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BOU = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BOU
			diplomatic_influence = { who = BOU value = -100 }
		}
	}

	gtfo_BRD = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BRD = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BRD = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BRD
			diplomatic_influence = { who = BRD value = -100 }
		}
	}

	gtfo_BRT = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BRT = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BRT = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BRT
			diplomatic_influence = { who = BRT value = -100 }
		}
	}

	gtfo_BRU = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BRU = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BRU = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BRU
			diplomatic_influence = { who = BRU value = -100 }
		}
	}

	gtfo_BRY = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BRY = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BRY = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BRY
			diplomatic_influence = { who = BRY value = -100 }
		}
	}

	gtfo_BSG = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BSG = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BSG = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BSG
			diplomatic_influence = { who = BSG value = -100 }
		}
	}

	gtfo_BTN = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BTN = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BTN = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BTN
			diplomatic_influence = { who = BTN value = -100 }
		}
	}

	gtfo_BTU = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BTU = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BTU = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BTU
			diplomatic_influence = { who = BTU value = -100 }
		}
	}

	gtfo_BUG = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BUG = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BUG = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BUG
			diplomatic_influence = { who = BUG value = -100 }
		}
	}

	gtfo_BUN = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BUN = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BUN = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BUN
			diplomatic_influence = { who = BUN value = -100 }
		}
	}

	gtfo_BUR = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BUR = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BUR = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BUR
			diplomatic_influence = { who = BUR value = -100 }
		}
	}

	gtfo_BYZ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			BYZ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			BYZ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = BYZ
			diplomatic_influence = { who = BYZ value = -100 }
		}
	}

	gtfo_CAM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CAM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CAM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CAM
			diplomatic_influence = { who = CAM value = -100 }
		}
	}

	gtfo_CAS = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CAS = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CAS = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CAS
			diplomatic_influence = { who = CAS value = -100 }
		}
	}

	gtfo_CCQ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CCQ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CCQ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CCQ
			diplomatic_influence = { who = CCQ value = -100 }
		}
	}

	gtfo_CDL = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CDL = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CDL = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CDL
			diplomatic_influence = { who = CDL value = -100 }
		}
	}

	gtfo_CEQ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CEQ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CEQ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CEQ
			diplomatic_influence = { who = CEQ value = -100 }
		}
	}

	gtfo_CGS = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CGS = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CGS = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CGS
			diplomatic_influence = { who = CGS value = -100 }
		}
	}

	gtfo_CHA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CHA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CHA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CHA
			diplomatic_influence = { who = CHA value = -100 }
		}
	}

	gtfo_CHC = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CHC = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CHC = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CHC
			diplomatic_influence = { who = CHC value = -100 }
		}
	}

	gtfo_CHG = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CHG = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CHG = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CHG
			diplomatic_influence = { who = CHG value = -100 }
		}
	}

	gtfo_CHK = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CHK = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CHK = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CHK
			diplomatic_influence = { who = CHK value = -100 }
		}
	}

	gtfo_CHM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CHM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CHM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CHM
			diplomatic_influence = { who = CHM value = -100 }
		}
	}

	gtfo_CHU = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CHU = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CHU = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CHU
			diplomatic_influence = { who = CHU value = -100 }
		}
	}

	gtfo_CIR = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CIR = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CIR = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CIR
			diplomatic_influence = { who = CIR value = -100 }
		}
	}

	gtfo_CJA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CJA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CJA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CJA
			diplomatic_influence = { who = CJA value = -100 }
		}
	}

	gtfo_CKW = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CKW = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CKW = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CKW
			diplomatic_influence = { who = CKW value = -100 }
		}
	}

	gtfo_CLM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CLM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CLM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CLM
			diplomatic_influence = { who = CLM value = -100 }
		}
	}

	gtfo_CMA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CMA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CMA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CMA
			diplomatic_influence = { who = CMA value = -100 }
		}
	}

	gtfo_CMI = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CMI = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CMI = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CMI
			diplomatic_influence = { who = CMI value = -100 }
		}
	}

	gtfo_CRA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CRA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CRA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CRA
			diplomatic_influence = { who = CRA value = -100 }
		}
	}

	gtfo_CRI = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CRI = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CRI = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CRI
			diplomatic_influence = { who = CRI value = -100 }
		}
	}

	gtfo_CRO = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CRO = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CRO = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CRO
			diplomatic_influence = { who = CRO value = -100 }
		}
	}

	gtfo_CRS = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CRS = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CRS = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CRS
			diplomatic_influence = { who = CRS value = -100 }
		}
	}

	gtfo_CSH = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CSH = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CSH = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CSH
			diplomatic_influence = { who = CSH value = -100 }
		}
	}

	gtfo_CSK = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CSK = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CSK = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CSK
			diplomatic_influence = { who = CSK value = -100 }
		}
	}

	gtfo_CSU = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CSU = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CSU = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CSU
			diplomatic_influence = { who = CSU value = -100 }
		}
	}

	gtfo_CWU = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CWU = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CWU = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CWU
			diplomatic_influence = { who = CWU value = -100 }
		}
	}

	gtfo_CXI = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CXI = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CXI = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CXI
			diplomatic_influence = { who = CXI value = -100 }
		}
	}

	gtfo_CYA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CYA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CYA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CYA
			diplomatic_influence = { who = CYA value = -100 }
		}
	}

	gtfo_CYI = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			CYI = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			CYI = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = CYI
			diplomatic_influence = { who = CYI value = -100 }
		}
	}

	gtfo_DAI = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			DAI = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			DAI = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = DAI
			diplomatic_influence = { who = DAI value = -100 }
		}
	}

	gtfo_DEN = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			DEN = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			DEN = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = DEN
			diplomatic_influence = { who = DEN value = -100 }
		}
	}

	gtfo_DLH = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			DLH = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			DLH = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = DLH
			diplomatic_influence = { who = DLH value = -100 }
		}
	}

	gtfo_DLI = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			DLI = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			DLI = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = DLI
			diplomatic_influence = { who = DLI value = -100 }
		}
	}

	gtfo_EGY = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			EGY = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			EGY = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = EGY
			diplomatic_influence = { who = EGY value = -100 }
		}
	}

	gtfo_EJZ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			EJZ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			EJZ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = EJZ
			diplomatic_influence = { who = EJZ value = -100 }
		}
	}

	gtfo_ENG = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ENG = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ENG = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ENG
			diplomatic_influence = { who = ENG value = -100 }
		}
	}

	gtfo_ETH = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ETH = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ETH = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ETH
			diplomatic_influence = { who = ETH value = -100 }
		}
	}

	gtfo_FEO = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			FEO = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			FEO = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = FEO
			diplomatic_influence = { who = FEO value = -100 }
		}
	}

	gtfo_FIN = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			FIN = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			FIN = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = FIN
			diplomatic_influence = { who = FIN value = -100 }
		}
	}

	gtfo_FLA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			FLA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			FLA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = FLA
			diplomatic_influence = { who = FLA value = -100 }
		}
	}

	gtfo_FRA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			FRA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			FRA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = FRA
			diplomatic_influence = { who = FRA value = -100 }
		}
	}

	gtfo_FUL = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			FUL = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			FUL = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = FUL
			diplomatic_influence = { who = FUL value = -100 }
		}
	}

	gtfo_GEO = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			GEO = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			GEO = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = GEO
			diplomatic_influence = { who = GEO value = -100 }
		}
	}

	gtfo_GER = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			GER = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			GER = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = GER
			diplomatic_influence = { who = GER value = -100 }
		}
	}

	gtfo_GOC = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			GOC = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			GOC = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = GOC
			diplomatic_influence = { who = GOC value = -100 }
		}
	}

	gtfo_GRA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			GRA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			GRA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = GRA
			diplomatic_influence = { who = GRA value = -100 }
		}
	}

	gtfo_GRE = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			GRE = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			GRE = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = GRE
			diplomatic_influence = { who = GRE value = -100 }
		}
	}

	gtfo_GRH = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			GRH = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			GRH = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = GRH
			diplomatic_influence = { who = GRH value = -100 }
		}
	}

	gtfo_GUG = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			GUG = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			GUG = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = GUG
			diplomatic_influence = { who = GUG value = -100 }
		}
	}

	gtfo_GUJ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			GUJ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			GUJ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = GUJ
			diplomatic_influence = { who = GUJ value = -100 }
		}
	}

	gtfo_HAN = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			HAN = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			HAN = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = HAN
			diplomatic_influence = { who = HAN value = -100 }
		}
	}

	gtfo_HAR = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			HAR = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			HAR = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = HAR
			diplomatic_influence = { who = HAR value = -100 }
		}
	}

	gtfo_HDJ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			HDJ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			HDJ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = HDJ
			diplomatic_influence = { who = HDJ value = -100 }
		}
	}

	gtfo_HDR = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			HDR = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			HDR = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = HDR
			diplomatic_influence = { who = HDR value = -100 }
		}
	}

	gtfo_HES = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			HES = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			HES = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = HES
			diplomatic_influence = { who = HES value = -100 }
		}
	}

	gtfo_HJA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			HJA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			HJA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = HJA
			diplomatic_influence = { who = HJA value = -100 }
		}
	}

	gtfo_HOD = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			HOD = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			HOD = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = HOD
			diplomatic_influence = { who = HOD value = -100 }
		}
	}

	gtfo_HSC = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			HSC = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			HSC = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = HSC
			diplomatic_influence = { who = HSC value = -100 }
		}
	}

	gtfo_HST = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			HST = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			HST = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = HST
			diplomatic_influence = { who = HST value = -100 }
		}
	}

	gtfo_HUA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			HUA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			HUA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = HUA
			diplomatic_influence = { who = HUA value = -100 }
		}
	}

	gtfo_ICL = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ICL = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ICL = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ICL
			diplomatic_influence = { who = ICL value = -100 }
		}
	}

	gtfo_ICM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ICM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ICM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ICM
			diplomatic_influence = { who = ICM value = -100 }
		}
	}

	gtfo_IRE = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			IRE = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			IRE = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = IRE
			diplomatic_influence = { who = IRE value = -100 }
		}
	}

	gtfo_IRQ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			IRQ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			IRQ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = IRQ
			diplomatic_influence = { who = IRQ value = -100 }
		}
	}

	gtfo_ISS = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ISS = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ISS = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ISS
			diplomatic_influence = { who = ISS value = -100 }
		}
	}

	gtfo_ITA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ITA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ITA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ITA
			diplomatic_influence = { who = ITA value = -100 }
		}
	}

	gtfo_ITZ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ITZ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ITZ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ITZ
			diplomatic_influence = { who = ITZ value = -100 }
		}
	}

	gtfo_JAP = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			JAP = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			JAP = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = JAP
			diplomatic_influence = { who = JAP value = -100 }
		}
	}

	gtfo_JAR = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			JAR = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			JAR = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = JAR
			diplomatic_influence = { who = JAR value = -100 }
		}
	}

	gtfo_JGD = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			JGD = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			JGD = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = JGD
			diplomatic_influence = { who = JGD value = -100 }
		}
	}

	gtfo_JIM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			JIM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			JIM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = JIM
			diplomatic_influence = { who = JIM value = -100 }
		}
	}

	gtfo_JIN = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			JIN = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			JIN = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = JIN
			diplomatic_influence = { who = JIN value = -100 }
		}
	}

	gtfo_JML = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			JML = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			JML = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = JML
			diplomatic_influence = { who = JML value = -100 }
		}
	}

	gtfo_JNN = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			JNN = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			JNN = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = JNN
			diplomatic_influence = { who = JNN value = -100 }
		}
	}

	gtfo_KAF = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KAF = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KAF = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KAF
			diplomatic_influence = { who = KAF value = -100 }
		}
	}

	gtfo_KAZ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KAZ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KAZ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KAZ
			diplomatic_influence = { who = KAZ value = -100 }
		}
	}

	gtfo_KCX = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KCX = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KCX = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KCX
			diplomatic_influence = { who = KCX value = -100 }
		}
	}

	gtfo_KER = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KER = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KER = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KER
			diplomatic_influence = { who = KER value = -100 }
		}
	}

	gtfo_KHA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KHA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KHA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KHA
			diplomatic_influence = { who = KHA value = -100 }
		}
	}

	gtfo_KIC = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KIC = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KIC = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KIC
			diplomatic_influence = { who = KIC value = -100 }
		}
	}

	gtfo_KIE = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KIE = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KIE = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KIE
			diplomatic_influence = { who = KIE value = -100 }
		}
	}

	gtfo_KLK = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KLK = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KLK = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KLK
			diplomatic_influence = { who = KLK value = -100 }
		}
	}

	gtfo_KMC = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KMC = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KMC = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KMC
			diplomatic_influence = { who = KMC value = -100 }
		}
	}

	gtfo_KON = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KON = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KON = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KON
			diplomatic_influence = { who = KON value = -100 }
		}
	}

	gtfo_KOR = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KOR = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KOR = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KOR
			diplomatic_influence = { who = KOR value = -100 }
		}
	}

	gtfo_KRC = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KRC = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KRC = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KRC
			diplomatic_influence = { who = KRC value = -100 }
		}
	}

	gtfo_KRN = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KRN = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KRN = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KRN
			diplomatic_influence = { who = KRN value = -100 }
		}
	}

	gtfo_KRW = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KRW = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KRW = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KRW
			diplomatic_influence = { who = KRW value = -100 }
		}
	}

	gtfo_KSJ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KSJ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KSJ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KSJ
			diplomatic_influence = { who = KSJ value = -100 }
		}
	}

	gtfo_KUB = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			KUB = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			KUB = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = KUB
			diplomatic_influence = { who = KUB value = -100 }
		}
	}

	gtfo_LAC = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			LAC = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			LAC = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = LAC
			diplomatic_influence = { who = LAC value = -100 }
		}
	}

	gtfo_LAN = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			LAN = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			LAN = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = LAN
			diplomatic_influence = { who = LAN value = -100 }
		}
	}

	gtfo_LBA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			LBA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			LBA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = LBA
			diplomatic_influence = { who = LBA value = -100 }
		}
	}

	gtfo_LGR = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			LGR = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			LGR = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = LGR
			diplomatic_influence = { who = LGR value = -100 }
		}
	}

	gtfo_LNA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			LNA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			LNA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = LNA
			diplomatic_influence = { who = LNA value = -100 }
		}
	}

	gtfo_LNG = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			LNG = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			LNG = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = LNG
			diplomatic_influence = { who = LNG value = -100 }
		}
	}

	gtfo_LOM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			LOM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			LOM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = LOM
			diplomatic_influence = { who = LOM value = -100 }
		}
	}

	gtfo_LOU = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			LOU = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			LOU = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = LOU
			diplomatic_influence = { who = LOU value = -100 }
		}
	}

	gtfo_LSK = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			LSK = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			LSK = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = LSK
			diplomatic_influence = { who = LSK value = -100 }
		}
	}

	gtfo_LUA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			LUA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			LUA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = LUA
			diplomatic_influence = { who = LUA value = -100 }
		}
	}

	gtfo_LUN = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			LUN = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			LUN = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = LUN
			diplomatic_influence = { who = LUN value = -100 }
		}
	}

	gtfo_LUZ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			LUZ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			LUZ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = LUZ
			diplomatic_influence = { who = LUZ value = -100 }
		}
	}

	gtfo_LXA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			LXA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			LXA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = LXA
			diplomatic_influence = { who = LXA value = -100 }
		}
	}

	gtfo_MAW = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MAW = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MAW = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MAW
			diplomatic_influence = { who = MAW value = -100 }
		}
	}

	gtfo_MBL = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MBL = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MBL = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MBL
			diplomatic_influence = { who = MBL value = -100 }
		}
	}

	gtfo_MCA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MCA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MCA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MCA
			diplomatic_influence = { who = MCA value = -100 }
		}
	}

	gtfo_MDO = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MDO = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MDO = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MDO
			diplomatic_influence = { who = MDO value = -100 }
		}
	}

	gtfo_MEW = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MEW = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MEW = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MEW
			diplomatic_influence = { who = MEW value = -100 }
		}
	}

	gtfo_MHR = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MHR = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MHR = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MHR
			diplomatic_influence = { who = MHR value = -100 }
		}
	}

	gtfo_MIX = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MIX = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MIX = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MIX
			diplomatic_influence = { who = MIX value = -100 }
		}
	}

	gtfo_MJP = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MJP = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MJP = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MJP
			diplomatic_influence = { who = MJP value = -100 }
		}
	}

	gtfo_MJZ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MJZ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MJZ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MJZ
			diplomatic_influence = { who = MJZ value = -100 }
		}
	}

	gtfo_MKS = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MKS = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MKS = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MKS
			diplomatic_influence = { who = MKS value = -100 }
		}
	}

	gtfo_MKU = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MKU = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MKU = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MKU
			diplomatic_influence = { who = MKU value = -100 }
		}
	}

	gtfo_MLC = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MLC = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MLC = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MLC
			diplomatic_influence = { who = MLC value = -100 }
		}
	}

	gtfo_MLI = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MLI = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MLI = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MLI
			diplomatic_influence = { who = MLI value = -100 }
		}
	}

	gtfo_MLT = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MLT = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MLT = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MLT
			diplomatic_influence = { who = MLT value = -100 }
		}
	}

	gtfo_MLW = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MLW = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MLW = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MLW
			diplomatic_influence = { who = MLW value = -100 }
		}
	}

	gtfo_MNX = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MNX = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MNX = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MNX
			diplomatic_influence = { who = MNX value = -100 }
		}
	}

	gtfo_MOR = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MOR = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MOR = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MOR
			diplomatic_influence = { who = MOR value = -100 }
		}
	}

	gtfo_MPH = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MPH = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MPH = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MPH
			diplomatic_influence = { who = MPH value = -100 }
		}
	}

	gtfo_MRI = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MRI = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MRI = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MRI
			diplomatic_influence = { who = MRI value = -100 }
		}
	}

	gtfo_MSH = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MSH = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MSH = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MSH
			diplomatic_influence = { who = MSH value = -100 }
		}
	}

	gtfo_MTL = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MTL = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MTL = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MTL
			diplomatic_influence = { who = MTL value = -100 }
		}
	}

	gtfo_MYR = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MYR = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MYR = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MYR
			diplomatic_influence = { who = MYR value = -100 }
		}
	}

	gtfo_MYS = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			MYS = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			MYS = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = MYS
			diplomatic_influence = { who = MYS value = -100 }
		}
	}

	gtfo_NDO = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			NDO = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			NDO = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = NDO
			diplomatic_influence = { who = NDO value = -100 }
		}
	}

	gtfo_NEJ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			NEJ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			NEJ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = NEJ
			diplomatic_influence = { who = NEJ value = -100 }
		}
	}

	gtfo_NET = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			NET = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			NET = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = NET
			diplomatic_influence = { who = NET value = -100 }
		}
	}

	gtfo_NEW = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			NEW = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			NEW = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = NEW
			diplomatic_influence = { who = NEW value = -100 }
		}
	}

	gtfo_NHX = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			NHX = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			NHX = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = NHX
			diplomatic_influence = { who = NHX value = -100 }
		}
	}

	gtfo_NKO = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			NKO = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			NKO = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = NKO
			diplomatic_influence = { who = NKO value = -100 }
		}
	}

	gtfo_NNG = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			NNG = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			NNG = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = NNG
			diplomatic_influence = { who = NNG value = -100 }
		}
	}

	gtfo_NOG = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			NOG = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			NOG = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = NOG
			diplomatic_influence = { who = NOG value = -100 }
		}
	}

	gtfo_NOR = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			NOR = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			NOR = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = NOR
			diplomatic_influence = { who = NOR value = -100 }
		}
	}

	gtfo_NOV = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			NOV = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			NOV = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = NOV
			diplomatic_influence = { who = NOV value = -100 }
		}
	}

	gtfo_NRM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			NRM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			NRM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = NRM
			diplomatic_influence = { who = NRM value = -100 }
		}
	}

	gtfo_NUP = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			NUP = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			NUP = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = NUP
			diplomatic_influence = { who = NUP value = -100 }
		}
	}

	gtfo_NVK = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			NVK = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			NVK = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = NVK
			diplomatic_influence = { who = NVK value = -100 }
		}
	}

	gtfo_OMA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			OMA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			OMA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = OMA
			diplomatic_influence = { who = OMA value = -100 }
		}
	}

	gtfo_ORI = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ORI = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ORI = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ORI
			diplomatic_influence = { who = ORI value = -100 }
		}
	}

	gtfo_OTM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			OTM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			OTM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = OTM
			diplomatic_influence = { who = OTM value = -100 }
		}
	}

	gtfo_OTO = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			OTO = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			OTO = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = OTO
			diplomatic_influence = { who = OTO value = -100 }
		}
	}

	gtfo_OYO = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			OYO = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			OYO = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = OYO
			diplomatic_influence = { who = OYO value = -100 }
		}
	}

	gtfo_PAP = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PAP = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PAP = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PAP
			diplomatic_influence = { who = PAP value = -100 }
		}
	}

	gtfo_PCH = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PCH = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PCH = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PCH
			diplomatic_influence = { who = PCH value = -100 }
		}
	}

	gtfo_PCJ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PCJ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PCJ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PCJ
			diplomatic_influence = { who = PCJ value = -100 }
		}
	}

	gtfo_PER = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PER = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PER = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PER
			diplomatic_influence = { who = PER value = -100 }
		}
	}

	gtfo_PEU = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PEU = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PEU = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PEU
			diplomatic_influence = { who = PEU value = -100 }
		}
	}

	gtfo_PGS = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PGS = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PGS = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PGS
			diplomatic_influence = { who = PGS value = -100 }
		}
	}

	gtfo_PHA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PHA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PHA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PHA
			diplomatic_influence = { who = PHA value = -100 }
		}
	}

	gtfo_PHG = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PHG = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PHG = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PHG
			diplomatic_influence = { who = PHG value = -100 }
		}
	}

	gtfo_PLB = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PLB = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PLB = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PLB
			diplomatic_influence = { who = PLB value = -100 }
		}
	}

	gtfo_PLT = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PLT = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PLT = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PLT
			diplomatic_influence = { who = PLT value = -100 }
		}
	}

	gtfo_PNJ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PNJ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PNJ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PNJ
			diplomatic_influence = { who = PNJ value = -100 }
		}
	}

	gtfo_POL = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			POL = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			POL = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = POL
			diplomatic_influence = { who = POL value = -100 }
		}
	}

	gtfo_POM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			POM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			POM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = POM
			diplomatic_influence = { who = POM value = -100 }
		}
	}

	gtfo_POR = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			POR = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			POR = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = POR
			diplomatic_influence = { who = POR value = -100 }
		}
	}

	gtfo_PRM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PRM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PRM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PRM
			diplomatic_influence = { who = PRM value = -100 }
		}
	}

	gtfo_PRO = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PRO = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PRO = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PRO
			diplomatic_influence = { who = PRO value = -100 }
		}
	}

	gtfo_PRT = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PRT = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PRT = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PRT
			diplomatic_influence = { who = PRT value = -100 }
		}
	}

	gtfo_PRU = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PRU = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PRU = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PRU
			diplomatic_influence = { who = PRU value = -100 }
		}
	}

	gtfo_PSA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			PSA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			PSA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = PSA
			diplomatic_influence = { who = PSA value = -100 }
		}
	}

	gtfo_QIC = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			QIC = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			QIC = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = QIC
			diplomatic_influence = { who = QIC value = -100 }
		}
	}

	gtfo_QTO = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			QTO = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			QTO = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = QTO
			diplomatic_influence = { who = QTO value = -100 }
		}
	}

	gtfo_RDL = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			RDL = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			RDL = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = RDL
			diplomatic_influence = { who = RDL value = -100 }
		}
	}

	gtfo_RWA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			RWA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			RWA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = RWA
			diplomatic_influence = { who = RWA value = -100 }
		}
	}

	gtfo_RYA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			RYA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			RYA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = RYA
			diplomatic_influence = { who = RYA value = -100 }
		}
	}

	gtfo_SAX = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SAX = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SAX = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SAX
			diplomatic_influence = { who = SAX value = -100 }
		}
	}

	gtfo_SHA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SHA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SHA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SHA
			diplomatic_influence = { who = SHA value = -100 }
		}
	}

	gtfo_SHU = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SHU = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SHU = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SHU
			diplomatic_influence = { who = SHU value = -100 }
		}
	}

	gtfo_SHY = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SHY = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SHY = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SHY
			diplomatic_influence = { who = SHY value = -100 }
		}
	}

	gtfo_SIA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SIA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SIA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SIA
			diplomatic_influence = { who = SIA value = -100 }
		}
	}

	gtfo_SIN = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SIN = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SIN = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SIN
			diplomatic_influence = { who = SIN value = -100 }
		}
	}

	gtfo_SKA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SKA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SKA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SKA
			diplomatic_influence = { who = SKA value = -100 }
		}
	}

	gtfo_SLS = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SLS = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SLS = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SLS
			diplomatic_influence = { who = SLS value = -100 }
		}
	}

	gtfo_SOL = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SOL = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SOL = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SOL
			diplomatic_influence = { who = SOL value = -100 }
		}
	}

	gtfo_SOM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SOM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SOM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SOM
			diplomatic_influence = { who = SOM value = -100 }
		}
	}

	gtfo_SOO = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SOO = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SOO = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SOO
			diplomatic_influence = { who = SOO value = -100 }
		}
	}

	gtfo_SRD = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SRD = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SRD = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SRD
			diplomatic_influence = { who = SRD value = -100 }
		}
	}

	gtfo_SRI = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SRI = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SRI = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SRI
			diplomatic_influence = { who = SRI value = -100 }
		}
	}

	gtfo_SUL = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SUL = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SUL = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SUL
			diplomatic_influence = { who = SUL value = -100 }
		}
	}

	gtfo_SWA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SWA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SWA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SWA
			diplomatic_influence = { who = SWA value = -100 }
		}
	}

	gtfo_SWE = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SWE = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SWE = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SWE
			diplomatic_influence = { who = SWE value = -100 }
		}
	}

	gtfo_SWI = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SWI = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SWI = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SWI
			diplomatic_influence = { who = SWI value = -100 }
		}
	}

	gtfo_SYC = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SYC = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SYC = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SYC
			diplomatic_influence = { who = SYC value = -100 }
		}
	}

	gtfo_SYR = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			SYR = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			SYR = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = SYR
			diplomatic_influence = { who = SYR value = -100 }
		}
	}

	gtfo_TAB = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			TAB = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			TAB = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = TAB
			diplomatic_influence = { who = TAB value = -100 }
		}
	}

	gtfo_TBZ = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			TBZ = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			TBZ = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = TBZ
			diplomatic_influence = { who = TBZ value = -100 }
		}
	}

	gtfo_TDO = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			TDO = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			TDO = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = TDO
			diplomatic_influence = { who = TDO value = -100 }
		}
	}

	gtfo_TIB = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			TIB = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			TIB = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = TIB
			diplomatic_influence = { who = TIB value = -100 }
		}
	}

	gtfo_TLA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			TLA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			TLA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = TLA
			diplomatic_influence = { who = TLA value = -100 }
		}
	}

	gtfo_TLC = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			TLC = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			TLC = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = TLC
			diplomatic_influence = { who = TLC value = -100 }
		}
	}

	gtfo_TOT = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			TOT = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			TOT = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = TOT
			diplomatic_influence = { who = TOT value = -100 }
		}
	}

	gtfo_TRA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			TRA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			TRA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = TRA
			diplomatic_influence = { who = TRA value = -100 }
		}
	}

	gtfo_TRS = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			TRS = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			TRS = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = TRS
			diplomatic_influence = { who = TRS value = -100 }
		}
	}

	gtfo_TRT = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			TRT = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			TRT = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = TRT
			diplomatic_influence = { who = TRT value = -100 }
		}
	}

	gtfo_TRX = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			TRX = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			TRX = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = TRX
			diplomatic_influence = { who = TRX value = -100 }
		}
	}

	gtfo_TTE = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			TTE = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			TTE = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = TTE
			diplomatic_influence = { who = TTE value = -100 }
		}
	}

	gtfo_TUL = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			TUL = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			TUL = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = TUL
			diplomatic_influence = { who = TUL value = -100 }
		}
	}

	gtfo_TUN = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			TUN = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			TUN = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = TUN
			diplomatic_influence = { who = TUN value = -100 }
		}
	}

	gtfo_UCA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			UCA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			UCA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = UCA
			diplomatic_influence = { who = UCA value = -100 }
		}
	}

	gtfo_UKR = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			UKR = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			UKR = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = UKR
			diplomatic_influence = { who = UKR value = -100 }
		}
	}

	gtfo_USA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			USA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			USA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = USA
			diplomatic_influence = { who = USA value = -100 }
		}
	}

	gtfo_UTS = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			UTS = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			UTS = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = UTS
			diplomatic_influence = { who = UTS value = -100 }
		}
	}

	gtfo_WES = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			WES = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			WES = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = WES
			diplomatic_influence = { who = WES value = -100 }
		}
	}

	gtfo_WHA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			WHA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			WHA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = WHA
			diplomatic_influence = { who = WHA value = -100 }
		}
	}

	gtfo_X00 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X00 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X00 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X00
			diplomatic_influence = { who = X00 value = -100 }
		}
	}

	gtfo_X01 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X01 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X01 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X01
			diplomatic_influence = { who = X01 value = -100 }
		}
	}

	gtfo_X02 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X02 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X02 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X02
			diplomatic_influence = { who = X02 value = -100 }
		}
	}

	gtfo_X03 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X03 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X03 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X03
			diplomatic_influence = { who = X03 value = -100 }
		}
	}

	gtfo_X04 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X04 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X04 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X04
			diplomatic_influence = { who = X04 value = -100 }
		}
	}

	gtfo_X05 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X05 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X05 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X05
			diplomatic_influence = { who = X05 value = -100 }
		}
	}

	gtfo_X06 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X06 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X06 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X06
			diplomatic_influence = { who = X06 value = -100 }
		}
	}

	gtfo_X07 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X07 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X07 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X07
			diplomatic_influence = { who = X07 value = -100 }
		}
	}

	gtfo_X08 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X08 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X08 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X08
			diplomatic_influence = { who = X08 value = -100 }
		}
	}

	gtfo_X09 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X09 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X09 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X09
			diplomatic_influence = { who = X09 value = -100 }
		}
	}

	gtfo_X10 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X10 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X10 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X10
			diplomatic_influence = { who = X10 value = -100 }
		}
	}

	gtfo_X11 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X11 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X11 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X11
			diplomatic_influence = { who = X11 value = -100 }
		}
	}

	gtfo_X12 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X12 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X12 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X12
			diplomatic_influence = { who = X12 value = -100 }
		}
	}

	gtfo_X13 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X13 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X13 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X13
			diplomatic_influence = { who = X13 value = -100 }
		}
	}

	gtfo_X14 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X14 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X14 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X14
			diplomatic_influence = { who = X14 value = -100 }
		}
	}

	gtfo_X15 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X15 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X15 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X15
			diplomatic_influence = { who = X15 value = -100 }
		}
	}

	gtfo_X16 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X16 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X16 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X16
			diplomatic_influence = { who = X16 value = -100 }
		}
	}

	gtfo_X17 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X17 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X17 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X17
			diplomatic_influence = { who = X17 value = -100 }
		}
	}

	gtfo_X18 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X18 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X18 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X18
			diplomatic_influence = { who = X18 value = -100 }
		}
	}

	gtfo_X19 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X19 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X19 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X19
			diplomatic_influence = { who = X19 value = -100 }
		}
	}

	gtfo_X20 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X20 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X20 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X20
			diplomatic_influence = { who = X20 value = -100 }
		}
	}

	gtfo_X21 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X21 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X21 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X21
			diplomatic_influence = { who = X21 value = -100 }
		}
	}

	gtfo_X22 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X22 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X22 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X22
			diplomatic_influence = { who = X22 value = -100 }
		}
	}

	gtfo_X23 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X23 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X23 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X23
			diplomatic_influence = { who = X23 value = -100 }
		}
	}

	gtfo_X24 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X24 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X24 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X24
			diplomatic_influence = { who = X24 value = -100 }
		}
	}

	gtfo_X25 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X25 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X25 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X25
			diplomatic_influence = { who = X25 value = -100 }
		}
	}

	gtfo_X26 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X26 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X26 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X26
			diplomatic_influence = { who = X26 value = -100 }
		}
	}

	gtfo_X27 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X27 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X27 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X27
			diplomatic_influence = { who = X27 value = -100 }
		}
	}

	gtfo_X28 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X28 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X28 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X28
			diplomatic_influence = { who = X28 value = -100 }
		}
	}

	gtfo_X29 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X29 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X29 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X29
			diplomatic_influence = { who = X29 value = -100 }
		}
	}

	gtfo_X30 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X30 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X30 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X30
			diplomatic_influence = { who = X30 value = -100 }
		}
	}

	gtfo_X31 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X31 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X31 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X31
			diplomatic_influence = { who = X31 value = -100 }
		}
	}

	gtfo_X32 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X32 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X32 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X32
			diplomatic_influence = { who = X32 value = -100 }
		}
	}

	gtfo_X33 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X33 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X33 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X33
			diplomatic_influence = { who = X33 value = -100 }
		}
	}

	gtfo_X34 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X34 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X34 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X34
			diplomatic_influence = { who = X34 value = -100 }
		}
	}

	gtfo_X35 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X35 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X35 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X35
			diplomatic_influence = { who = X35 value = -100 }
		}
	}

	gtfo_X36 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X36 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X36 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X36
			diplomatic_influence = { who = X36 value = -100 }
		}
	}

	gtfo_X37 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X37 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X37 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X37
			diplomatic_influence = { who = X37 value = -100 }
		}
	}

	gtfo_X38 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X38 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X38 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X38
			diplomatic_influence = { who = X38 value = -100 }
		}
	}

	gtfo_X39 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X39 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X39 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X39
			diplomatic_influence = { who = X39 value = -100 }
		}
	}

	gtfo_X40 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X40 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X40 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X40
			diplomatic_influence = { who = X40 value = -100 }
		}
	}

	gtfo_X41 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X41 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X41 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X41
			diplomatic_influence = { who = X41 value = -100 }
		}
	}

	gtfo_X42 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X42 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X42 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X42
			diplomatic_influence = { who = X42 value = -100 }
		}
	}

	gtfo_X43 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X43 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X43 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X43
			diplomatic_influence = { who = X43 value = -100 }
		}
	}

	gtfo_X44 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X44 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X44 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X44
			diplomatic_influence = { who = X44 value = -100 }
		}
	}

	gtfo_X45 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X45 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X45 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X45
			diplomatic_influence = { who = X45 value = -100 }
		}
	}

	gtfo_X46 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X46 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X46 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X46
			diplomatic_influence = { who = X46 value = -100 }
		}
	}

	gtfo_X47 = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			X47 = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			X47 = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = X47
			diplomatic_influence = { who = X47 value = -100 }
		}
	}

	gtfo_XAL = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			XAL = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			XAL = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = XAL
			diplomatic_influence = { who = XAL value = -100 }
		}
	}

	gtfo_XIU = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			XIU = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			XIU = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = XIU
			diplomatic_influence = { who = XIU value = -100 }
		}
	}

	gtfo_XMA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			XMA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			XMA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = XMA
			diplomatic_influence = { who = XMA value = -100 }
		}
	}

	gtfo_YAN = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			YAN = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			YAN = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = YAN
			diplomatic_influence = { who = YAN value = -100 }
		}
	}

	gtfo_YEM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			YEM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			YEM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = YEM
			diplomatic_influence = { who = YEM value = -100 }
		}
	}

	gtfo_YKA = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			YKA = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			YKA = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = YKA
			diplomatic_influence = { who = YKA value = -100 }
		}
	}

	gtfo_YOK = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			YOK = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			YOK = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = YOK
			diplomatic_influence = { who = YOK value = -100 }
		}
	}

	gtfo_ZAP = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ZAP = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ZAP = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ZAP
			diplomatic_influence = { who = ZAP value = -100 }
		}
	}

	gtfo_ZIM = {
		picture = gtfo
		alert = no

		potential = {
			ai = no
			has_country_flag = gtfo
			ZIM = {
				vassal_of = THIS
				NOT = { substate_of = THIS }
			}
		}

		allow = {
			war = no
			ZIM = { war = no }
		}

		effect = {
			prestige_factor = -0.1
			release_vassal = ZIM
			diplomatic_influence = { who = ZIM value = -100 }
		}
	}
}
